# Procfile required to deploy your streamlit app to heroku
# web: sh setup.sh && streamlit run <name-of-streamlit-app>.py
# replace with your correct filename's streamlit app e.g app.py, my_super_app.py
# mine is called app.py
web: sh setup.sh && streamlit run app.py